<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>CRUD application</title>
</head>
<body class="w-full h-full bg-zinc-900">
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500 " href="/read">Read Users</a>
        </div>

        <h1 class="text-3xl  tracking-trigher mb-3">Update User</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-600 border-redius rounded-lg bg-transparent text-white outline-none " type="text" name="name" placeholder="name" value="<%= user.name %>" >
            <input class="px-5 py-2 border-2 border-zinc-600 border-redius rounded-lg bg-transparent text-white outline-none " type="text" name="email" placeholder="email" value="<%= user.email %>">
            <input class="px-5 py-2 border-2 border-zinc-600 border-redius rounded-lg bg-transparent text-white outline-none " type="text" name="image" placeholder="image" value="<%= user.images %>">
            <input class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3" type="submit" value="Update">
        </form>
    </div>
</body>
</html>