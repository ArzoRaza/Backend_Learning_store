<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>mini projects</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end mb-10">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm " href="/logout">Logout</a>
        </div>
        <div>
            <div class="flex items-start gap-3 ">
                <div class="w-10 h-10  rounded-md overflow-cover">
                    <img class="w-full h-full object-cover" src="/images/upload/<%= user.profilephoto %> " alt="">
                </div>
                <h1 class="text-3xl " > <span class="font-light">Hello</span> <%= user.name %>👋🏻👋🏻</h1>

            </div>
                <h5 class="mb-5">You can create a post </h5>
            <form method="post" action="/post">
                <textarea placeholder="write your things" class="p-3 mb-3 block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" ></textarea>
                <input class="px-3 w-40 py-2 bg-blue-500  rounded-md block" type="submit" value="create new post">
            </form>
        </div>  

    <div class="posts mt-20">
        <h3 class="text-zinc-400 mb-3">Your Posts</h3>
        <div class="postcontainer  mt-5 flex gap-2">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-4 rounded-md border-[1px] border-zinc-600 bg-transparent bg-zinc-800">
                    <h4 class="text-yellow-400" > <%= user.username%> </h4>
                    <p class="text-sm tracking-tight"> <%= post.content %> </p>
                    <small class="mt-4 inline-block"><%= post.likes.length %> likes</small>
                    
                    <div class="btns flex flex-wrap space-x-28  mt-1">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-500" href="/edit/<%= post._id %>">edit</a>
                    </div>
                </div>
            <% }) %>
            
        </div>

    </div>

    
</body>
</html>